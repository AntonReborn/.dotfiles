- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.ssh/config:
        path: ssh/config
        create: true
    ~/.tmux.conf: tmux/tmux.conf
    ~/.config/utils/:
        path: utils/
        create: true
    ~/.config/fish/:
        path: fish/
        create: true
        force: true
    ~/.config/kitty/:
        path: kitty/
        create: true
    ~/.config/nvim/:
        path: neovim/
        create: true

- create:
  - ~/Projects 
  - ~/RustroverProjects 
  - ~/CLionProjects

- shell:
  - [git submodule update --init --recursive, Installing submodules]



